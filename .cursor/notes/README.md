# 项目问题与方案备忘

本目录用于记录 **Shipyard 仓库在实现/联调中遇到的具体问题、根因与落地做法**，与 **版本规划**（[`.cursor/plans/`](../plans/) 中的需求规格、路线图）区分存放。

- **适合写在这里**：某端（如小程序）壳层/主题/构建的踩坑、与第三方平台时序相关的结论、可复用的代码锚点。
- **不要写在这里**：下一版范围、里程碑、P0/P1 需求条目（仍用 `plans` 内对应需求规格与路线图）。

新增备忘时可按主题起文件名（如 `mp-*.md`），并在文内用相对路径链接到相关 `plans` 文档（若有关）。
